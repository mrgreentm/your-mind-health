spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ymh?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ymh
    password: ymh
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
